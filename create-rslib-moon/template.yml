title: 'create-rslib-moon'
destination: 'packages/[name]'
description: |  
  Scaffolds the initial structure for an npm package, including source and test folders, a package.json, and more.
variables:  
  name:  
    type: 'string'
    default: 'rslib-project'
    required: true
    prompt: 'Package name?' 
